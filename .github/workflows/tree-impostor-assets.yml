name: Tree impostor assets

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - .github/workflows/tree-impostor-assets.yml
      - editor.config.yaml
      - public/assets/grass-scene.glb
      - public/assets/textures/bark/**
      - public/assets/textures/leaf/foliage_512a.png
      - public/assets/trees/**
      - scripts/bake-tree-impostors.mjs
      - scripts/validate-impostors.mjs
      - src/editor/stylized/AuthoredTextureNode.js
      - src/editor/stylized/StylizedNoiseNodes.js
      - src/editor/stylized/StylizedPrototypeBake.js
      - src/editor/stylized/StylizedTreeMaterials.js
      - src/editor/stylized/StylizedTreePrototypes.js
      - src/editor/stylized/StylizedTreeView.js
      - src/editor/stylized/TreeLodAssembler.js
      - src/editor/stylized/forest/ForestSpeciesGeometry.js
      - src/editor/stylized/forest/ProceduralBarkTextures.js
      - src/editor/stylized/forest/TreeWindTime.js
      - src/editor/stylized/forest/proceduralGeometry.js
      - src/editor/stylized/impostor/**
  pull_request:
    paths:
      - .github/workflows/tree-impostor-assets.yml
      - editor.config.yaml
      - public/assets/grass-scene.glb
      - public/assets/textures/bark/**
      - public/assets/textures/leaf/foliage_512a.png
      - public/assets/trees/**
      - scripts/bake-tree-impostors.mjs
      - scripts/validate-impostors.mjs
      - src/editor/stylized/AuthoredTextureNode.js
      - src/editor/stylized/StylizedNoiseNodes.js
      - src/editor/stylized/StylizedPrototypeBake.js
      - src/editor/stylized/StylizedTreeMaterials.js
      - src/editor/stylized/StylizedTreePrototypes.js
      - src/editor/stylized/StylizedTreeView.js
      - src/editor/stylized/TreeLodAssembler.js
      - src/editor/stylized/forest/ForestSpeciesGeometry.js
      - src/editor/stylized/forest/ProceduralBarkTextures.js
      - src/editor/stylized/forest/TreeWindTime.js
      - src/editor/stylized/forest/proceduralGeometry.js
      - src/editor/stylized/impostor/**

permissions:
  contents: write

concurrency:
  group: tree-impostor-assets-${{ github.event.pull_request.number || 'main' }}
  cancel-in-progress: true

jobs:
  bake:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout event revision
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.16.0'

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Locate Chromium
        shell: bash
        run: |
          set -euo pipefail
          browser="$(command -v google-chrome || command -v google-chrome-stable || command -v chromium || command -v chromium-browser || true)"
          if [[ -z "$browser" ]]; then
            echo "No Chromium-family browser is installed on the runner." >&2
            exit 1
          fi
          echo "CHROME_PATH=$browser" >> "$GITHUB_ENV"
          "$browser" --version

      - name: Bake offline impostor atlases
        run: npm run bake:impostors

      - name: Verify generated assets and application
        run: npm run verify

      - name: Upload generated atlases
        uses: actions/upload-artifact@v4
        with:
          name: offline-tree-impostors
          path: public/assets/impostors/trees
          if-no-files-found: error
          retention-days: 7

      - name: Commit generated assets
        if: github.event_name != 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A -- public/assets/impostors/trees
          if git diff --cached --quiet; then
            echo "Offline tree impostor assets are already current."
            exit 0
          fi
          git commit -m "build: refresh offline tree impostor atlases"
          git pull --rebase origin main
          git push origin HEAD:main
