{
  "name": "simcity-dnd",
  "private": true,
  "version": "0.11.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate:forest-assets": "node scripts/generate-forest-assets.mjs",
    "validate:forest-assets": "node scripts/validate-forest-assets.mjs",
    "bake:impostors": "node scripts/bake-tree-impostors.mjs",
    "validate:impostors": "node scripts/validate-impostors.mjs",
    "validate:impostors:required": "node scripts/validate-impostors.mjs --required",
    "validate:assets": "node scripts/validate-assets.mjs && npm run validate:forest-assets && npm run validate:impostors",
    "validate:assets:production": "node scripts/validate-assets.mjs && npm run validate:forest-assets && npm run validate:impostors:required",
    "test": "node --test",
    "qa:perf": "node scripts/run-perf-qa.mjs",
    "qa:perf:parse": "node scripts/parse-perf-qa.mjs tmp/perf-qa-latest.json",
    "verify": "npm run validate:assets:production && npm test && npm run build"
  },
  "dependencies": {
    "js-yaml": "4.3.0",
    "three": "0.185.1"
  },
  "devDependencies": {
    "vite": "8.1.5"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
